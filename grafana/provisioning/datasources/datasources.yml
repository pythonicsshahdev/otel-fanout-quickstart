apiVersion: 1

datasources:
  - name: OpenSearch Logs
    uid: opensearch-logs
    type: grafana-opensearch-datasource
    access: proxy
    url: http://opensearch:9200
    isDefault: true
    jsonData:
      timeField: ingestion_timestamp
      version: "3.0.0"
      flavor: opensearch
      logMessageField: message
      logLevelField: severity_number
      database: boomi-logs*
      derivedFields:
        - name: TraceID
          matcherType: field
          matcherQuery: trace_id
          url: "${__value.raw}"
          datasourceUid: opensearch-traces
          urlDisplayLabel: View Trace
    editable: true

  - name: OpenSearch Metrics
    uid: opensearch-metrics
    type: grafana-opensearch-datasource
    access: proxy
    url: http://opensearch:9200
    jsonData:
      timeField: timestamp
      version: "3.0.0"
      flavor: opensearch
      database: boomi-metrics*
    editable: true

  - name: OpenSearch Traces
    uid: opensearch-traces
    type: grafana-opensearch-datasource
    access: proxy
    url: http://opensearch:9200
    jsonData:
      timeField: startTime
      version: "3.0.0"
      flavor: opensearch
      database: ss4o_traces-*
    editable: true

  - name: OTel Collector
    uid: otel-collector-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    jsonData:
      timeInterval: 15s
    editable: true
